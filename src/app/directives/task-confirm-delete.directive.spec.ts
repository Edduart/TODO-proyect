import { TaskConfirmDeleteDirective } from './task-confirm-delete.directive';

describe('TaskConfirmDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskConfirmDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
